/**
 * Firebase Services - Cloud Storage for Miru
 * 
 * These services provide cloud sync for watchlist and watch history
 * when the user is authenticated. Falls back to localStorage for guests.
 */

export * from './watchlist';
export * from './watchHistory';
